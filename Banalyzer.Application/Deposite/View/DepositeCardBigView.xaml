<UserControl x:Class="Banalyzer.Application.Deposite.View.DepositeCardBigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <DockPanel>
    <!--<DockPanel Background="{Binding Currency, Converter={StaticResource CurrencyToColorConverter}}">-->
        <TextBlock DockPanel.Dock="Top" Text="Owner" />
        <TextBlock DockPanel.Dock="Top" Text="{Binding Owner}" />

        <TextBlock DockPanel.Dock="Top" Text="Opened Amount" />
        <TextBlock DockPanel.Dock="Top" Text="{Binding OpenedAmount}" />

        <TextBlock DockPanel.Dock="Top" Text="Current Amount" />
        <TextBlock DockPanel.Dock="Top" Text="{Binding CurrentAmount}" />

        <TextBlock DockPanel.Dock="Top" Text="Period" />
        <Grid DockPanel.Dock="Top">
            <TextBlock HorizontalAlignment="Stretch" Text="{Binding OpenedDate}" />
            <TextBlock HorizontalAlignment="Stretch" Text="{Binding CloseDate}" />
        </Grid>

        <TextBlock DockPanel.Dock="Top" HorizontalAlignment="Stretch" Text="Percent"/>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Percent}"/>
    </DockPanel>
</UserControl>
